#并查集

````
1. 合并两个集合
2. 查询某个元素的祖宗节点

优化:路径压缩:指向祖宗节点
按秩合并:把节点数量少的树,挂在节点数量多的树上
O(log n)每次合并,或查询

两个扩展:
记录每个集合的大小,将集合的大小绑定到根节点

带权并查集
每个点到根节点的距离,绑定到每个元素身上
食物链,我们用元素到根节点的距离,来获得元素之间的相对关系
用带权并查集维护相对距离

食物链也可以用扩展域并查集
如果有k类动物:那么扩展域并查集是O(k),而带权并查集不受影响
如果k不大,那么没有区别,
拓展域是枚举思想.枚举当前元素如果是第一类干点啥,或者第二类干点啥...
