###字符串
```
1.有无重复字符
给定一串字符abcdedfa1321.，、,返回有无重复字符

2.翻转字符串

3.变形词，给定2个字符串，其中一个能否变成另一个字符串
考虑空格与大小写

4.替换空格
将字符串中的空格全部换成%20

5.字符串压缩，或者字符串统计
aaabccc压缩成a3b1c3
压缩后的字符串没有变短，就返回原来的字符串

6.两个字符串是否由相同的字符组成

7.旋转词
给定两个字符串，判定s2能否s1通过作循环移位rotate得到的字符串包括
比如
AEBCD -> EBCD A  ->  BCD AE  ->CD AEB  ->D AEBC  -> AEBCD
后面移位产生的字符串，s2是否是其旋转产生的子串。
a->b
b+b是否包含a
思路，b+b把移位的部分，移到前面去了，其中如果包含a，就说明可以通过循环移位得到

8.将字符串按单词翻转，如将：here you are翻转城are you here

9.去掉字符串中连续出现k次的0，正则。。。

10.回文串，，找最大回文串，如何去切，能切出最多的回文串，比如密码脱离ABD本来是回文多少次
翻转后和自己对比看是否相等

11.最短摘要，比如，尺取法
包含给定字符串的最短摘要。

###RabinKarp
hash->滚动hash
计算字符串的hash值。比如有三位，C0*31^2+C1*31^1+C2
但写起来是 ((((0+C0)*31)+C1)*31+C2)*31


###KMP
暴力匹配，效率太低
字符串
求Next数组

###后缀数组
把字符串所有的后缀列出来，排序O(n2logn)
子串一定是某个后缀的前缀
空间换时间
所以使用倍增法求后缀数组，更高效
先对每1个字符进行排序，再扩展成2个字符，4个字符，每一次都要利用上一次的信息

